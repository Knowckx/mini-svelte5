# 项目进度

## 2026-06-24

- 新增 `/file-system-access` 教学测试页。
- 已覆盖 File System Access API 的能力检测、单文件读取、目录读取、文件写入、目录子项删除、OPFS 沙盒读写/删除。
- 删除教学使用 `FileSystemDirectoryHandle.removeEntry()` 作为主路径；`FileSystemHandle.remove()` 仅在 README 中作为实验/非标准能力说明。
- 未运行项目或测试。

## 2026-07-03

- 移除 SvelteKit 壳，迁移为普通 Svelte 5 + Vite 单页应用。
- 新增 `index.html`、`src/main.ts`、`src/App.svelte`，用轻量前端路由切换首页和 `/file-system-access`。
- 首页迁移到 `src/pages/HomePage.svelte`，File System Access 页面迁移到 `src/pages/FileSystemAccessPage.svelte`。
- 移除 Kit 专属配置和 PWA 插件/更新提示，保留 Tailwind、mkcert、`infa-s5` 本地包处理。
- 未运行项目或测试。

## 2026-07-03

- 接入普通 Vite 版 PWA：使用 `vite-plugin-pwa`，更新策略为 `registerType: 'prompt'`。
- 恢复 `src/lib/components/PWAUpdatePrompt.svelte`，在 `src/App.svelte` 中全局挂载，提示新版本并由用户手动刷新。
- PWA 配置保留 manifest、图标、截图、旧缓存清理和 SPA 导航 fallback。
- 未运行项目或测试。
